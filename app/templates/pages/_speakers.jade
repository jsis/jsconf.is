extends ../shared/_page

block append vars
  - path = '/speakers';
  - blurb = 'Final speaker lineup!'

block icon
  include ../../images/podium.svg

block title
  | Speakers

block expanded
  .SpeakerList(data-speakers)
    each speaker in speakers
      .Speaker(style="background-image: url(/images/speakers/" + speaker.slug + ".jpg); touch-action: manipulation;" alt=speaker.name data-speaker=speaker.slug)
        .Speaker-info
          h3.Speaker-name= speaker.name
          h4.Speaker-company!= speaker.company || '&nbsp;'
